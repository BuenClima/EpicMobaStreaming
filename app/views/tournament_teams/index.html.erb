<p id="notice"><%= notice %></p>

<h1>Tournament Teams</h1>

<table>
  <thead>
    <tr>
      <th>Tournament</th>
      <th>Team</th>
      <th>Created at</th>
      <th>Updated at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tournament_teams.each do |tournament_team| %>
      <tr>
        <td><%= tournament_team.tournament %></td>
        <td><%= tournament_team.team %></td>
        <td><%= tournament_team.created_at %></td>
        <td><%= tournament_team.updated_at %></td>
        <td><%= link_to 'Show', tournament_team %></td>
        <td><%= link_to 'Edit', edit_tournament_team_path(tournament_team) %></td>
        <td><%= link_to 'Destroy', tournament_team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tournament Team', new_tournament_team_path %>
