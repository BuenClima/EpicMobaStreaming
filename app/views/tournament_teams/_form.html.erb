<%= form_with(model: tournament_team, local: true) do |form| %>
  <% if tournament_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament_team.errors.count, "error") %> prohibited this tournament_team from being saved:</h2>

      <ul>
      <% tournament_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tournament_id %>
    <%= form.text_field :tournament_id, id: :tournament_team_tournament_id %>
  </div>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.text_field :team_id, id: :tournament_team_team_id %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.datetime_select :created_at, id: :tournament_team_created_at %>
  </div>

  <div class="field">
    <%= form.label :updated_at %>
    <%= form.datetime_select :updated_at, id: :tournament_team_updated_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
